{
  "name": "midi-listener",
  "version": "0.0.1",
  "description": "Listen for and interpret MIDI device input",
  "main": "build/MidiListener.js",
  "types": "build/MidiListener.d.ts",
  "scripts": {
    "build": "rimraf build && tsc && echo Build complete.",
    "test": "echo Not test driven, lol && exit 0"
  },
  "author": "Chase Moskal <chasemoskal@gmail.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/ChaseMoskal/midi-listener.git"
  },
  "devDependencies": {
    "@types/core-js": "*",
    "rimraf": "*",
    "typescript": "^2.1"
  }
}